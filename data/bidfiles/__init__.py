"""Writing the desk's downstream market bid files — one module per market,
since each market's official spreadsheet has its own format. `swpw.py` is
the first; see PROJECT.md for what's built and what's still open."""
