[pytest]
testpaths = tests
markers =
    db: needs a live, read-only DB connection (the app's own SQL Server for the WECC calendar, and/or MAGAPPSERVER for bilateral trades) — skipped by default, run with --run-db to include.
